pub struct VoiceActor {
    pub name: String,
    pub reading: String,
}

pub struct Character {
    pub name: String,
    pub reading: String,
    pub voice_actor: String,
}

pub struct Unit {
    pub name: String,
    pub reading: String,
}
